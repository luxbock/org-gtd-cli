# Python bytecode / pytest caches
__pycache__/
*.py[cod]
.pytest_cache/

# Hypothesis scratch state (the COMMITTED example DB lives in
# .hypothesis-examples/, configured in conftest.py)
.hypothesis/

# Byte-compiled elisp
*.elc

# trash-cli local fallback trash (created when `trash` runs on this mount)
.Trash-*/

# Claude Code session state (agent worktrees, local settings)
.claude/
